# پلن ساخت Gravity Room

پلتفرم هدف: **Meta Quest 3**

مسیر فنی پیشنهادی: **Unity 6 + URP + Meta XR Interaction SDK**

هر فاز با یک خروجی قابل‌تست تمام می‌شود. اولویت اجرایی، فازهای ۱ تا ۴ است؛ خروجی آن‌ها اولین نسخهٔ کامل و قابل‌بازی خواهد بود.

## فاز ۱ — راه‌اندازی پروژه

- [x] بررسی Unity، ابزارهای Android و اتصال Quest 3
  - مدل: GPT-6؛ شناسهٔ دقیق مدل: نامشخص — در این اجرا اعلام نشده؛ ابزار: Unity CLI و adb.
  - توکن: نامشخص — آمار دقیق مصرف این تسک در دسترس نیست.
  - بررسی: Unity 6000.0.83f1 و Quest 3 شناسایی شدند. ناسازگاری ابزارهای همراه نصب پیدا شد و مسیر Java 17 و NDK r27c موجود روی سیستم انتخاب شد.
- [x] ساخت پروژهٔ Unity 6 با URP
  - مدل‌ها: GPT-6 (شناسهٔ دقیق نامشخص) و `gpt-5.6-sol`؛ ابزار: Unity CLI و Codex CLI.
  - توکن هر مدل برای این آیتم به‌تنهایی: نامشخص — اجرای Sol چند آیتم را پوشش داده؛ مصرف آن فقط در زیرتسک ابزار تنظیم پروژه ثبت شده تا دوباره شمرده نشود.
  - بررسی: پروژه با Unity 6000.0.83f1 و URP 17.0.4 ساخته شد و اجرای تنظیمات در Unity موفق بود.
  - [x] پیاده‌سازی ابزار تنظیم پروژه، تولید صحنه، اعتبارسنجی و ساخت Android
    - مدل: `gpt-5.6-sol`؛ ابزار: Codex CLI. تکمیل و رفع اشکال بعدی توسط GPT-6 با شناسهٔ دقیق نامشخص.
    - توکن Sol: ورودی ۳۳۱۱۱۱۷، خروجی ۲۱۸۶۰، مجموع ۳۳۳۲۹۷۷؛ از ورودی، ۳۱۹۳۷۲۸ توکن کش‌شده است و دوباره به مجموع اضافه نمی‌شود.
    - منبع: رویداد `turn.completed` در Codex CLI؛ ثبت‌شده در `docs/model-usage.json`. توکن GPT-6 برای تکمیل و رفع اشکال: نامشخص.
    - بررسی: کد کامپایل شد؛ تولید صحنه و اعتبارسنجی پیکربندی در Unity اجرا شد. ساخت و اجرای APK در آیتم جداگانه پیگیری می‌شود.
- [x] راه‌اندازی Git و تنظیم فایل‌های نادیده‌گرفته‌شده
  - مدل: GPT-6؛ شناسهٔ دقیق مدل: نامشخص — در این اجرا اعلام نشده؛ ابزار: Git CLI.
  - توکن هر مدل: نامشخص — آمار دقیق مصرف این تسک در دسترس نیست.
  - بررسی: مخزن روی شاخهٔ main راه‌اندازی شد و نادیده‌گرفتن کش Unity، خروجی ساخت و فایل‌های محرمانهٔ متداول بررسی شد.
- [x] نصب نسخه‌های سازگار Meta XR و Interaction SDK
  - مدل‌ها: GPT-6 (شناسهٔ دقیق نامشخص) و `gpt-5.6-sol`؛ ابزار: Unity Package Manager و Codex CLI.
  - توکن هر مدل برای این آیتم به‌تنهایی: نامشخص — مصرف اجرای مشترک Sol در زیرتسک ابزار تنظیم پروژه ثبت شده است.
  - بررسی: Meta Core، Interaction و OVR Integration نسخهٔ 205.0.0 و OpenXR نسخهٔ 1.16.1 نصب و کامپایل شدند؛ نسخه‌های وابستگی‌ها قفل شدند.
- [x] تنظیم ورودی کنترلرها و دست‌های مجازی
  - مدل‌ها: GPT-6 با شناسهٔ دقیق نامشخص؛ اجرای مشترک `gpt-5.6-sol` برای rig؛ تلاش تشخیص با `gemini-3.8-flash-medium` از طریق agy.
  - توکن GPT-6: نامشخص؛ مصرف Sol در زیرتسک ابزار تنظیم پروژه ثبت شده و سهم این آیتم نامشخص است.
  - توکن Gemini برای تلاش تشخیص: ورودی ۳۸۲۰۴۲، خروجی ۱۵۱۰۷، مجموع گزارش‌شده ۳۹۷۱۴۹؛ منبع: `docs/model-usage.json`. اجرای agy با پایان مهلت پاسخ نهایی نداد؛ رفع اشکال توسط GPT-6 انجام شد.
  - بررسی: فعال‌سازی مجوز دست‌ها؛ هر دو OVRHand دادهٔ معتبر با confidence بالا دارند؛ تصویر زندهٔ Quest هر دو دست نیمه‌شفاف دور کنترلرها را نشان داد؛ تغییر Trigger و Grip هر دو دست در لاگ ثبت شد.
- [x] ساخت صحنهٔ آزمایشی با مقیاس صحیح
  - مدل‌ها: `gpt-5.6-sol` و GPT-6 با شناسهٔ دقیق نامشخص.
  - توکن هر مدل برای این آیتم: نامشخص؛ مصرف مشترک Sol فقط در زیرتسک ابزار تنظیم پروژه ثبت شده است.
  - بررسی: اتاق ۶×۶ متر با ارتفاع ۲٫۵ متر و مکعب مرجع یک‌متری؛ مبدأ کف؛ تصویر استریو روی Quest، نوشته‌های خوانا و نبود حرکت مصنوعی.
- [x] گرفتن خروجی APK و اجرای آن روی هدست
  - مدل‌ها: GPT-6 با شناسهٔ دقیق نامشخص، `gpt-5.6-sol` برای ابزار ساخت و Gemini از طریق agy برای اسکریپت نصب.
  - توکن هر مدل برای این آیتم: نامشخص؛ مصرف CLIها در زیرتسک‌های اختصاصی ثبت شده و دوباره شمرده نمی‌شود.
  - بررسی: APK ساخته شد؛ صفر خطا و ۸ هشدار؛ نصب و اجرای موفق روی Quest 3؛ OpenXR فعال، سر و کنترلرها ردیابی شدند و پس از برداشتن/پوشیدن هدست دوباره داده و تصویر دریافت شد.
  - [x] آماده‌سازی و بررسی اسکریپت‌های ساخت و نصب روی macOS
    - مدل‌ها: GPT-6 (شناسهٔ دقیق نامشخص) برای نسخهٔ اولیه؛ `gemini-3.8-flash-medium` از طریق `agy CLI` برای بررسی و اصلاح.
    - توکن GPT-6: نامشخص — آمار دقیق این تسک در دسترس نیست.
    - توکن Gemini: ورودی ۲۳۰۴۰۴، خروجی ۲۸۴۲۱، مجموع گزارش‌شده ۲۵۸۸۲۵؛ منبع: دادهٔ usage در خروجی JSON ابزار agy، ثبت‌شده در `docs/model-usage.json`.
    - بررسی: سازگاری Bash 3.2، انتخاب صریح دستگاه، مدیریت خطای اجرای برنامه و بررسی نحوی اسکریپت‌ها.

**معیار پایان:** صحنه روی Quest اجرا شود و حرکت سر و کنترلرها درست باشد.

## فاز ۲ — گرفتن و پرتاب

**وضعیت:** پیاده‌سازی و بررسی رایانه‌ای انجام شده؛ پذیرش نهایی روی Quest و تنظیم حس پرتاب باز است.

اجرای مشترک پیاده‌سازی با `gpt-5.6-sol` از طریق Codex CLI: ورودی ۱٬۴۶۴٬۵۶۱، خروجی ۲۱٬۵۳۶، مجموع ۱٬۴۸۶٬۰۹۷ توکن. این مصرف فقط یک‌بار برای مجموعهٔ تسک‌های زیر حساب می‌شود؛ تفکیک هر آیتم در خروجی ابزار موجود نیست. منبع: `docs/model-usage.json`.

- [x] ساخت اتاق ساده با کف و دیوارهای دارای برخورد
  - مدل‌ها: `gpt-5.6-sol` (پیاده‌سازی) و GPT-6 با شناسهٔ دقیق نامشخص (بازبینی/اصلاح/اعتبارسنجی).
  - توکن هر آیتم: Sol نامشخص به‌صورت تفکیکی؛ جزو اجرای مشترک بالا. GPT-6 نامشخص؛ آمار تسک در دسترس نیست.
  - بررسی: کف و دیوارهای فاز ۱ در صحنهٔ فاز ۲ حفظ شدند؛ تست سقوط و برخورد با دیوار در PhysX پاس شد.
- [x] ساخت سکوی تولید اشیا در دسترس بازیکن
  - مدل‌ها: `gpt-5.6-sol` (پیاده‌سازی) و GPT-6 با شناسهٔ دقیق نامشخص (بازبینی/اصلاح/اعتبارسنجی).
  - توکن هر آیتم: Sol نامشخص به‌صورت تفکیکی؛ جزو اجرای مشترک بالا. GPT-6 نامشخص؛ آمار تسک در دسترس نیست.
  - بررسی: سکوی ۸۵ سانتی‌متری، توپ با شعاع ۱۰ سانتی‌متر؛ دسترسی واقعی روی هدست هنوز باید تأیید شود.
- [x] اضافه‌کردن یک شیء قابل‌گرفتن با هر دو دست، به‌صورت جداگانه
  - مدل‌ها: `gpt-5.6-sol` (پیاده‌سازی) و GPT-6 با شناسهٔ دقیق نامشخص (بازبینی/اصلاح/اعتبارسنجی).
  - توکن هر آیتم: Sol نامشخص به‌صورت تفکیکی؛ جزو اجرای مشترک بالا. GPT-6 نامشخص؛ آمار تسک در دسترس نیست.
  - بررسی: اتصال HandGrab و Grab به SDK، یک نقطهٔ گرفتن و انتقال به دست دوم؛ تست حضوری در آیتم آخر باز است.
- [x] پیاده‌سازی رهاسازی و انتقال سرعت پرتاب
  - مدل‌ها: `gpt-5.6-sol` (پیاده‌سازی) و GPT-6 با شناسهٔ دقیق نامشخص (بازبینی/اصلاح/اعتبارسنجی).
  - توکن هر آیتم: Sol نامشخص به‌صورت تفکیکی؛ جزو اجرای مشترک بالا. GPT-6 نامشخص؛ آمار تسک در دسترس نیست.
  - بررسی: ThrowWhenUnselected و Rigidbody متصل‌اند؛ لحظهٔ رهاسازی از رویداد SDK ثبت می‌شود. حس پرتاب هنوز روی هدست تأیید نشده است.
- [x] ساخت یک دروازه و تشخیص عبور صحیح شیء
  - مدل‌ها: `gpt-5.6-sol` (پیاده‌سازی) و GPT-6 با شناسهٔ دقیق نامشخص (بازبینی/اصلاح/اعتبارسنجی).
  - توکن هر آیتم: Sol نامشخص به‌صورت تفکیکی؛ جزو اجرای مشترک بالا. GPT-6 نامشخص؛ آمار تسک در دسترس نیست.
  - بررسی: عبور جاروب‌شدهٔ رو به جلو با احتساب شعاع؛ تست عبور سریع، جهت معکوس، بیرون دهانه و برخورد قاب پاس شد.
- [x] بازگرداندن اشیای دورافتاده یا خارج‌شده از اتاق
  - مدل‌ها: `gpt-5.6-sol` (پیاده‌سازی) و GPT-6 با شناسهٔ دقیق نامشخص (بازبینی/اصلاح/اعتبارسنجی).
  - توکن هر آیتم: Sol نامشخص به‌صورت تفکیکی؛ جزو اجرای مشترک بالا. GPT-6 نامشخص؛ آمار تسک در دسترس نیست.
  - بررسی: بازگشت پس از موفقیت، خروج، سکون دور از دسترس یا ۱۲ ثانیه؛ گرفتن دوباره تایمر قبلی را لغو می‌کند. تست رقابت گرفتن/بازگشت روی هدست باقی است.
- [ ] تنظیم نهایی اندازه، وزن و برخورد اشیا روی هدست
  - تنظیم اولیه: شعاع ۰٫۱ متر، جرم ۰٫۳۵ کیلوگرم، برخورد ContinuousDynamic؛ کف، دیوار و قاب در شبیه‌سازی پاس شدند.
- [ ] تست مکرر گرفتن و پرتاب روی هدست
  - طبق درخواست کاربر، هدست برای تماشای فیلم آزاد است؛ نصب و تست نسخهٔ فاز ۲ انجام نشده.
  - طراحی سناریوهای تست: `gemini-3.8-flash-medium` از طریق agy CLI؛ ورودی ۲۰٬۲۶۹، خروجی ۲٬۵۶۰، مجموع گزارش‌شده ۲۲٬۸۲۹ توکن. این مصرف مربوط به طراحی تست است، نه اجرای آن.

**معیار پایان:** بازیکن بتواند چند بار پشت‌سرهم شیء را بگیرد و با کنترل مناسب به هدف پرتاب کند. هنوز تأیید نشده است.

## فاز ۳ — مکانیک جاذبه

- [ ] ساخت سیستم جاذبهٔ اختصاصی برای اشیای آزاد
- [ ] اضافه‌کردن حالت‌های پایین، چپ و راست
- [ ] ثابت نگه‌داشتن اتاق و موقعیت دید بازیکن
- [ ] تعیین رفتار شیء هنگام گرفتن و رهاکردن در هر حالت
- [ ] اضافه‌کردن فلش نمایش جهت جاذبه
- [ ] اضافه‌کردن شمارش معکوس و صدای هشدار تغییر
- [ ] تنظیم شدت جاذبه و فاصلهٔ زمانی تغییرها
- [ ] تست مسیر پرتاب و برخوردها در هر سه حالت

**معیار پایان:** تغییر جاذبه قابل‌فهم باشد و بازیکن بتواند پرتابش را با آن تطبیق دهد.

## فاز ۴ — چرخهٔ کامل بازی

- [ ] ساخت سه دروازه با رنگ و نماد متفاوت
- [ ] اضافه‌کردن گوی انرژی و کپسول به‌عنوان دو نوع شیء
- [ ] تعیین هدف هر شیء با رنگ و نماد
- [ ] پیاده‌سازی امتیاز اصابت صحیح و بازخورد اصابت اشتباه
- [ ] اضافه‌کردن ضریب موفقیت‌های متوالی
- [ ] ساخت راند ۹۰ ثانیه‌ای
- [ ] افزایش تدریجی سختی در طول راند
- [ ] ساخت حالت‌های شروع، بازی، توقف و پایان
- [ ] نمایش نتیجه، ذخیرهٔ رکورد محلی و دکمهٔ بازی مجدد
- [ ] اضافه‌کردن آموزش کوتاه تعاملی

**معیار پایان:** یک راند کامل از شروع تا نمایش نتیجه بدون دخالت توسعه‌دهنده اجرا شود.

## فاز ۵ — ظاهر، صدا و حس تعامل

- [x] ساخت نسخهٔ موردتأیید دستکش و ساعد با الهام از مرجع — جلوتر از سایر کارهای فاز ۵
  - مدل‌ها: `gpt-5.6-sol` و GPT-6 با شناسهٔ دقیق نامشخص؛ آمار هر اجرای مشترک در زیرتسک‌های مربوط ثبت شده و دوباره جمع نمی‌شود. سهم مستقل این آیتم نامشخص است.
  - معیار پایان این نسخه: تأیید کاربر برای دستکش سفید و مشکی، ساعد تا آرنج و رفع تداخل سفیدی انگشت‌ها. تطابق دقیق با جزئیات تصویر مرجع ادعا نمی‌شود.
  - [x] پیاده‌سازی نسخهٔ اولیهٔ دستکش مات سفید و مشکی با نواحی تیرهٔ کف دست و مفاصل
    - مدل‌ها: `gpt-5.6-sol` برای پیاده‌سازی؛ GPT-6 با شناسهٔ دقیق نامشخص برای بازبینی، اصلاح و تست.
    - توکن اجرای مشترک Sol برای دستکش، متریال و ابزار تنظیم: ورودی ۴۱۲۵۳۲۴، خروجی ۲۳۶۲۸، مجموع ۴۱۴۸۹۵۲؛ ۴۰۳۱۴۸۸ توکن کش‌شده زیرمجموعهٔ ورودی است. سهم مستقل هر زیرتسک نامشخص است.
    - تلاش ناموفق قبلی Sol در حالت فقط‌خواندنی: ورودی ۳۸۶۴۹۷، خروجی ۲۵۷۵، مجموع ۳۸۹۰۷۲. مصرف تلاش اولیهٔ متوقف‌شده و GPT-6 نامشخص است؛ منبع آمار: `docs/model-usage.json`.
    - بررسی: تصویر زندهٔ Quest کف مشکی، پوشش سفید پشت دست و مچ مشکی را نشان داد. فرم فعلی ساده‌تر از مرجع است و تأیید ظاهر نهایی هنوز باز است.
  - [x] پیاده‌سازی مچ مشکی، ساعد و بازوی سفید تا شانه با تخمین آرنج از سر و دست
    - مدل: `gpt-5.6-sol`؛ بازبینی و ساخت: GPT-6 با شناسهٔ دقیق نامشخص.
    - توکن Sol: ورودی ۱۲۱۸۲۱۳، خروجی ۹۲۹۸، مجموع ۱۲۲۷۵۱۱؛ کش ۱۲۰۰۸۹۶ زیرمجموعهٔ ورودی است. توکن GPT-6 نامشخص؛ منبع: `docs/model-usage.json`.
    - بررسی: کامپایل و ساخت Android موفق؛ موقعیت آرنج و شانه تخمینی است. تست تصویری نسخهٔ بازو روی هدست هنوز در زیرتسک تأیید باقی است.
  - [x] مخفی‌کردن مدل کنترلر با حفظ ردیابی، ورودی و حرکت انگشت‌ها
    - مدل‌ها: `gpt-5.6-sol` و GPT-6 با شناسهٔ دقیق نامشخص؛ مصرف مشترک Sol در زیرتسک دستکش ثبت شده، سهم مستقل و توکن GPT-6 نامشخص است.
    - بررسی: فقط رندر کنترلر خاموش می‌شود و اشیای داده/تعامل فعال می‌مانند. ورودی دکمه‌ها در فاز ۱ تأیید شده بود؛ تست این اصلاح ظاهری با ردیابی دست و تأیید کاربر انجام شد.
  - [x] اصلاح متن دیوار تا دست‌های مات، نوشته‌های پشتشان را بپوشانند
    - مدل: GPT-6 با شناسهٔ دقیق نامشخص؛ توکن: نامشخص — آمار این تسک در دسترس نیست.
    - بررسی: شیدر متن با آزمون عمق و پشتیبانی استریو کامپایل شد و در APK جدید قرار گرفت؛ بررسی تصویر نهایی همراه بازو انجام می‌شود.
  - [x] اصلاح محدودهٔ آستین به ساعد تا آرنج و حفظ سفیدی انگشت‌ها بدون قطعات تداخل‌کننده
    - مدل: GPT-6 با شناسهٔ دقیق نامشخص؛ توکن: نامشخص — آمار دقیق این تسک در دسترس نیست.
    - بررسی: به درخواست کاربر بخش بالای آرنج حذف شد؛ سفیدی انگشت‌ها روی رأس‌های مش پوست قرار گرفت تا همراه انگشت خم شود. ساخت APK بدون خطا انجام شد و روی Quest نصب شد؛ تأیید تصویری هنوز باز است.
  - [x] بررسی نسخهٔ ساعد روی Quest و تأیید ظاهر توسط کاربر
    - مدل: GPT-6 با شناسهٔ دقیق نامشخص برای بررسی؛ تأیید بصری توسط کاربر.
    - توکن: نامشخص — آمار دقیق این تسک در دسترس نیست.
    - بررسی: کاربر نسخهٔ ساعد تا آرنج و اصلاح سفیدی انگشت‌ها را با «خوب شد الان» تأیید کرد. لاگ ایجاد نواحی سفید روی سطح هر دو دست و نبود خطای مدیریت‌شده در اجرای بررسی‌شده تأیید شد.

- [x] بررسی گزارش موقت خرابی دست چپ پس از تأیید نسخه
  - مدل‌ها: `gpt-5.6-sol` برای بازبینی فقط‌خواندنی؛ GPT-6 با شناسهٔ دقیق نامشخص برای تصویر و لاگ.
  - توکن Sol: ورودی ۱۱۱۸۶۸، خروجی ۲۰۰۴، مجموع ۱۱۳۸۷۲؛ کش ۸۱۶۶۴ زیرمجموعهٔ ورودی است. توکن GPT-6 نامشخص؛ منبع: `docs/model-usage.json`.
  - نتیجه: تصویر دو دست ثبت شد؛ کاربر سپس گفت «الان درسته». در این بررسی تغییری در کد یا APK اعمال نشد و رفع قطعی یک علت مشخص ادعا نمی‌شود.

- [ ] بررسی فایل‌ها و مجوزهای assetهای منتخب
- [ ] واردکردن بخش‌های موردنیاز Kenney Modular Space Kit و Factory Kit
- [ ] جایگزینی اتاق ساده با محیط فضایی
- [ ] طراحی ظاهر نهایی دو شیء با فرم قابل‌تشخیص
- [ ] ساخت متریال دروازه‌ها و علائم جاذبه
- [ ] تنظیم نورپردازی پخته و عناصر نورانی
- [ ] اضافه‌کردن صداهای گرفتن، پرتاب، اصابت و تغییر جاذبه
- [ ] اضافه‌کردن لرزش کنترلر و افکت موفقیت
- [ ] تنظیم خوانایی نوشته‌ها و اندازهٔ رابط در هدست

**معیار پایان:** یک راند با ظاهر و صدای منسجم اجرا شود و اتفاقات بازی واضح باشند.

## فاز ۶ — تست و بهینه‌سازی

- [ ] اندازه‌گیری عملکرد روی Quest 3 با هدف ۹۰ فریم پایدار
- [ ] بهینه‌سازی مدل‌ها، متریال‌ها، نورها و افکت‌ها
- [ ] محدودکردن تعداد اشیای فیزیکی فعال و استفادهٔ مجدد از آن‌ها
- [ ] بررسی عبور ناخواستهٔ پرتاب‌های سریع از دیوار یا هدف
- [ ] تست توقف، برداشتن هدست و برگشت به بازی
- [ ] تست دست غالب چپ و راست و دسترسی راحت به اشیا
- [ ] اجرای چند راند متوالی برای بررسی پایداری
- [ ] گرفتن بازخورد از چند بازیکن و تنظیم سختی و دقت پرتاب

**معیار پایان:** نسخه روی هدست پایدار باشد و ایرادهای اصلی تعامل و عملکرد رفع شده باشند.

## فاز ۷ — بستهٔ قابل‌ارائه

- [ ] رفع ایرادهای باقی‌ماندهٔ نسخهٔ اولیه
- [ ] تنظیم نام، آیکن و شمارهٔ نسخه
- [ ] آماده‌کردن APK نهایی قابل‌نصب
- [ ] ثبت نسخهٔ قابل‌بازگشت در Git
- [ ] نوشتن راهنمای کوتاه نصب و کنترل‌ها
- [ ] ضبط ویدیوی کوتاه از گیم‌پلی واقعی

**معیار پایان:** نسخه‌ای آماده باشد که بتوان به فرد دیگری داد تا نصب کند و بازی کند.

## فاز ۸ — توسعهٔ بعدی، پس از تأیید نسخهٔ اولیه

- [ ] بررسی و اضافه‌کردن Hand Tracking
- [ ] گسترش اشیا از دو نوع به شش نوع
- [ ] اضافه‌کردن بی‌وزنی و جهت‌های بیشتر جاذبه
- [ ] ساخت حالت تمرینی بدون تایمر
- [ ] طراحی چالش‌ها و چیدمان‌های جدید
- [ ] بررسی آماده‌سازی برای انتشار در فروشگاه Meta
